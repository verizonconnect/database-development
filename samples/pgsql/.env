# Connection Details
database_name=adventure_postgres
user=postgres
password=flyway
host_port=5432

# Database Container 
container_name=pgsql_container
image=../../tools/pgsql

# Flyway 
flyway_image=flyway/flyway:11-Alpine
# Flyway migrate command [migrate, repair, info]
flyway_command=migrate
default_schema=flyway
sql_migration_prefix=v
repeatable_sql_migration_prefix=r
mixed=true

# Flyway Placeholders
# These values will be substituted at deploy time
environment_name=build

# Sqlfluff rules generation
generate_rules_image=../../tools/generate_rules

# Sqlfluff
# static analysis controls
sqlfluff_image=../../tools/sqlfluff_rules
# lint_verbosity in [-v, -vv, -vvv, -vvvv]
lint_verbosity=-v
lint_output_path=/lint/sqlfluff.output

# Code Coverage
coverage_image=../../tools/cover_me

